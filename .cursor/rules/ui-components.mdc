---
description: Правила построения UI-компонентов и интерфейса проекта TRIVE
globs:
  - "frontend/**/*"
alwaysApply: true
---

🎨 **Руководство по UI и фронтенд-компонентам (React + Tailwind)**

1. Используется библиотека React (JSX), стиль — **TailwindCSS**.
2. Интерфейс строится из переиспользуемых компонентов: `EditorPanel`, `SeoBlock`, `ActionButtons`, `TextHistory`.
3. Все компоненты оформляются как функции:
   ```js
   const EditorPanel = () => { ... }
   ```
4. Стилизация исключительно через **Tailwind**. Не использовать внешние CSS-файлы.
5. Использовать осмысленные имена классов, переменных и пропсов (`textBlock`, `onStatusChange`, `seoFields`).
6. События оформлять с префиксом `handle`: `handleClick`, `handleApprove`, `handleEdit`
7. Компоненты UI должны включать обязательные кнопки:
   - `Согласовать` (Approve)
   - `Отправить на доработку` (RequestChanges)
   - `Удалить` (Delete)
8. Интерфейс должен поддерживать следующие поля:
   - `H1`, `метаописание`, `ключевые слова`
   - статус согласования (`draft`, `approved`, `rework`, `deleted`)
9. Компоненты должны быть адаптированы для будущей фильтрации по тематикам и статусам.
10. Каждый компонент должен быть легко масштабируем и переиспользуем.

Если структура интерфейса не утверждена — уточни у пользователя перед началом разработки.
